<p id="notice"><%= notice %></p>

<h1>有給申請</h1>



<%= link_to '有給申請する', new_public_calendar_path %>


<%= month_calendar events: @calendars do |date, events| %>
  <%= date.day %>
  <% events.each do |event| %>
    <div>
      <%= event.title %>
      
    </div>
  <% end %>
<% end %>


